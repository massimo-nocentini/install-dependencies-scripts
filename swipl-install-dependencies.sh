
# `swipl` compiling is halved in two steps: first build the core swipl kernel
# within the `src` folder, second build packages in `packages` folder.

sudo apt-get install libunwind-dev autoconf libgmp-dev unixodbc-dev openssl \
    zlib1g-dev libxt-dev libfontconfig1-dev pkg-config libxft-dev \
    libxpm-dev libjpeg-dev libarchive-dev
