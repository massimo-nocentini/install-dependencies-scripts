
sudo apt-get install binutils gcc make m4 perl tar build-essential dpkg-dev

# at the end of the compilation, the following message appears:
#To install small scripts to directly run Sage's versions of GAP, the PARI?GP
#interpreter, Maxima, or Singular etc. (by typing e.g. just 'gap' or 'gp') 
#into a standard 'bin' directory, start Sage by typing 'sage' (or './sage') 
#and enter something like 
#   `install_scripts('/usr/local/bin')`
#at the Sage command prompt ('sage:').

